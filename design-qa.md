# VisionSelect UI 设计 QA

## 对照范围

- 参考视觉：`C:\Users\Administrator\.codex\generated_images\019f49a3-a06f-7081-8dc1-bdfa6e098688\exec-e15e9d01-f878-4bf8-8530-ce43758d413d.png`
- 实现截图：`E:\code\VisionSelect\.codex_tmp\ui-audit-20260710\implementation-input-1440x1024-v2.png`
- 全屏对照：`E:\code\VisionSelect\.codex_tmp\ui-audit-20260710\design-comparison-full.png`
- 局部对照：`E:\code\VisionSelect\.codex_tmp\ui-audit-20260710\design-comparison-focus.png`
- 视口：1440 × 1024
- 页面状态：中文、需求输入页、默认参数、未执行选型

## 全屏对照结论

- 信息架构与参考视觉一致：深色顶栏、窄侧边导航、需求结构、参数编辑器、实时约束和底部命令区形成清晰的工程工作台。
- 视觉层级稳定：主操作使用工业蓝，成功状态使用绿色，风险提示使用琥珀色；边框、底色和文本对比保持克制。
- 1440 × 1024 下没有控件裁切、文本重叠、横向滚动或不可达操作。
- 当前软件的数据模型少于概念稿，因此实现保留真实字段和真实计算结果，没有为填满界面伪造工程参数。

## 局部对照结论

- 中部编辑器按“工件尺寸、精度指标、成像节拍与安装、工艺环境与光学偏好”组织，扫描路径和参考稿一致。
- 右侧实时约束已覆盖目标视场、物方像素、最低相机、带宽、工作距离、帧率、工艺输入和风险判断。
- 输入框、下拉框、复选框和按钮的高度、边框及焦点色统一；主要操作保持可点击并沿用现有选型流程。

## 检查记录

1. 首轮实现中通用背景规则覆盖了标签透明背景，已补充标签透明规则。
2. 1440 × 1024 首轮页面纵向信息偏少，已拆分工艺环境分组并补充实时工艺输入，使密度更接近参考稿。
3. 最终构建、QtTest 测试和 UTF-8 编码检查均通过。
4. 1280 × 820 默认窗口下已检查导航、三栏布局和主要文本，没有发现关键内容裁切。
5. 系统标题栏已隐藏，窗口控制统一收纳到深色顶栏；支持顶栏拖动、双击最大化/还原，以及最小化、最大化、关闭操作。
6. 普通窗口四角使用 12px 圆角裁剪；最大化和全屏状态自动清除窗口圆角，保持系统级满屏显示。

## 剩余差异

- 参考稿包含定位方式、装夹稳定性、MTF、置信水平等尚未进入现有业务模型的字段；本次不扩展选型算法和数据结构。
- 参考稿的侧栏图标更偏概念化；实现复用项目已有图标资源，避免引入风格不一致的临时资产。

## 最终结果

passed
