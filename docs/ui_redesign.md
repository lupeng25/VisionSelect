# 界面重构说明

页面切换卡顿的后续修复、计时口径及 Release 验证记录见 [页面切换性能修复](navigation_performance.md)。

## 设计方向

以成熟的工程工作台为目标：石墨黑导航与银灰工作区形成层次，低饱和蓝紫色集中用于当前导航、选中项与主要操作。减少全屏边框和实色按钮，统一线形图标、圆角、标题、辅助文字和数据间距。

## 页面调整

- 需求建模：移除重复的大纲栏，将尺寸、精度、节拍、光学偏好和备注组织成编号卡片。右侧展示随输入变化的工件与视场示意、核心指标及工程提示；底部固定“生成选型方案”操作。
- 参数校算：突出实际视场、物方像素和带宽利用率，完整计算依据保留在下方可滚动详情中。
- 候选计算：统一表格行高、表头、按钮与标题样式，减少网格线干扰。
- 方案评审：匹配度数值与解释文字分层，保留兼容状态、风险、产品组合及详细校核信息。
- 产品库：调整型号和参数的默认列宽；原有列宽设置仍优先恢复，筛选、分页和数据操作保持原有功能。
- 3D 相机：以型号结果表为主体，筛选条件默认收起，统计和型号管理集中到一行工具栏；型号详情按需展开，表格获得主要显示空间。

## 3D 结果表优先布局

2026-09-12 根据用户提供的两张截图，将 3D 产品筛选页从常驻表单改为结果表优先：

- 默认只显示标题、页签、紧凑工具栏和型号表，不常驻筛选表单或底部详情。1440 × 900 逻辑窗口可同时浏览约 13 行型号；窄窗口默认结果表占页面高度的 60% 以上。
- 点击“筛选条件”展开表单；入口显示条件数量和“未应用”状态。应用成功后自动收起，清空条件仍可在表单内操作。
- 型号管理保持在工具栏中，筛选区关闭时也可新增、复制、编辑和删除自定义型号。
- 单击选择型号，双击或按 Enter 展开底部详情；方向键切换行时详情及参数设定使用的当前相机同步更新。收起详情后表格重新获得空间。
- 应用筛选时按品牌和型号保留已有选中项；页面间来回切换保留筛选控件和选中型号。
- 默认列宽优先保证匹配状态和型号可读，长参数提供提示和横向滚动；表头布局使用新的版本键，不沿用旧布局中被压窄的列宽。

回归检查覆盖默认表格占比、筛选展开/应用/清空、高级条件控件适配、鼠标与键盘详情操作、选择保留，以及中英文和窄窗口。真实 Qt 渲染图通过 `VISIONSELECT_UI_CAPTURE_DIR` 输出，文件名前缀为 `three-d-`。

本次 Release 完整 UI CTest 在普通缩放下通过（18.93 秒），150% 缩放下通过（22.50 秒）；已手工检查结果表、展开筛选、高级条件、详情、中英文和窄窗口的真实 Qt 渲染图。Debug 与 Release 程序均已重新构建。

## 实现位置

- `resources/styles/base.qss`：字体、密度与通用控件结构。
- `resources/styles/light.qss`：普通主题的颜色、层次与组件状态。
- `src/ui/UiThemeManager.cpp`：统一样式、调色板及高对比模式。
- `src/ui/UiHelpers.cpp`：线形图标、通用卡片和表格行为。
- `src/ui/pages/`：需求页、结果页、校算页、产品库及 3D 页布局。

新需求页分隔器使用独立的设置键，避免旧三栏尺寸影响双栏布局；不删除用户旧设置。

## 验证方法

使用真实的 Windows Qt Widgets 渲染检查页面，不用网页模拟桌面界面。UI 回归覆盖导航、输入和备注保留、摘要显隐、异步计算完成、小窗口尺寸及语言切换；原有算法、导出和授权测试继续保留。

可通过以下方式输出页面截图。请先构建，并将 Qt 运行库目录加入 `PATH`。

```powershell
$env:VISIONSELECT_UI_CAPTURE_DIR = "$PWD\.codex_tmp\ui-review"
.\build\windows-msvc2022\bin\VisionSelectUiTests.exe -o .codex_tmp\ui-review-tests.log,txt
```

截图用于逐页检查标题、导航、布局、表格和文字适配。测试窗口采用不显示到桌面的方式渲染，因此这项验证不等同于真人操作窗口拖拽、系统贴靠或切换系统高对比模式；这些系统级交互仍可在实际部署环境补充验收。

## 本次验证记录

- 本地 Qt 6.10.1、MSVC 19.51 构建成功。
- 算法与导出测试组通过；更新后的 UI 测试记录为 10 项通过、0 项失败。
- 检查六个页面在 1440 × 900 和 1080 × 700 下的实际渲染，包含中英文输入页、导航折叠及舒适/紧凑密度。
- 在 150% 缩放下再次通过工作台交互与尺寸回归，并检查高分屏截图。
- 已检查卡片、标题、按钮与表格的文字适配；较长产品参数仍使用省略、提示和横向滚动，用户可调整列宽。
- 项目文本 UTF-8 检查及 `git diff --check` 通过。
